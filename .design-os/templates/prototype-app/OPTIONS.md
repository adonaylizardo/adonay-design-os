# Prototype options: __FEATURE__

| Option | Branch | Status | Port | Preview URL | Hypothesis | Vibe brief |
|--------|--------|--------|------|-------------|------------|------------|
| base | proto/__SLUG__/__FEATURE__/base | active | __PORT__ | — | Initial scaffold from program knowledge | — |

**Status:** draft | active | review | winner | rejected | archived

One option = one git branch. The `base` branch is the undeletable starting point.

## Rules

- Brand tokens come from `context/documents/` via `design knowledge` — see `BRAND.md`.
- Do not invent a client palette. Cite program knowledge and DS docs.
- Inbound stakeholder HTML lives in `insights/prototypes/` (`design vibe-check`), not here.
