# Agent notes — prototype __FEATURE__

Read before editing this outbound prototype:

1. `projects/__SLUG__/context/program-knowledge.md`
2. `projects/__SLUG__/context/prd.md`
3. `BRAND.md` and `src/tokens.css` in this folder
4. `OPTIONS.md` — one option = one branch

## Constraints

- Use CSS variables from `tokens.css` only. No ad-hoc hex unless cited in BRAND.md.
- This is a thinking tool to validate interaction logic from the PRD — not a deliverable.
- Inbound stakeholder prototypes are in `insights/prototypes/` — do not merge workflows.

## After validation

Generate `figma-handoff.md` in project insights when an option is validated (see design vibe / review workflow).
